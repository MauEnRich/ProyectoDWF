spring.application.name=control-escolar
# URL de conexi�n a la base de datos MySQL (ajusta el nombre de la base y host si es necesario)
spring.datasource.url=jdbc:mysql://localhost:3306/control_escolar?useSSL=false&serverTimezone=UTC

# Usuario y contrase�a para la conexi�n
spring.datasource.username=root
spring.datasource.password=

# Driver JDBC para MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Opcional: Dialecto para Hibernate (si usas JPA/Hibernate)
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Opcional: Mostrar las consultas SQL en consola
spring.jpa.show-sql=true

# Opcional: Actualizar autom�ticamente el esquema (crea/actualiza tablas)
spring.jpa.hibernate.ddl-auto=update

server.port=8080